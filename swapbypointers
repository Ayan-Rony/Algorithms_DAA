#include<stdio.h>
int swap(int *a,int *b);
int n;
void main()
{
    int a,b;
    printf("Enter");
    scanf("%d%d",&a,&b);
    swap(&a,&b);
    printf("%d\n%u",a,b);
    
}
int swap(int *a,int *b)
{
    n=*a;
    *a=*b;
    *b=n;
    return(*a,*b);

}
